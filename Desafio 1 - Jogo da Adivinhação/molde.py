import random

numero_aleatorio = random.randint(1, 20) #Usando o "random.randint()" podemos criar um número inteiro aleatório, e podemos passar 2 parâmetros
                                         #No primeiro parâmetro passamos o número inicial e no último o final, e ele irá criar um número entre esse intervalo

